// Interface outline only: selected signatures, not a compilable full class.
#pragma once

class UPzStorageBoxRPCComponent
{
public:
    void S_FastMoveoutStorage_Implementation(const FGuid& BoxGuid, const int SrcPos, const int Count);
    // State, helper declarations, reflection and base classes are external.
};
